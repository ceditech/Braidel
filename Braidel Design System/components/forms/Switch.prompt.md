On/off toggle for instant-apply settings.

```jsx
<Switch label="Available for work" defaultChecked />
<Switch size="sm" label="Email alerts" />
```

Use for state that applies immediately (availability, notifications). For form choices that submit later, use Checkbox.
