Read-only star rating in brand gold, with fractional fill.

```jsx
<Rating value={4.8} count={126} />
<Rating value={5} showValue={false} size="0.9rem" />
```

Supports fractional values (4.8 → partly-filled star). Shows the numeric value by default; pass `count` for the review total.
